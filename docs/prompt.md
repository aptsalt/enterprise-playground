 Create a TD checking account opening flow with eligibility check, document upload, and confirmation steps
    - Hits RAG hard (Accounts category), multi-step workflow, rich HTML/CSS/JS output
  2. Build a credit card comparison dashboard showing rewards, APR, annual fees, and an apply now CTA for each card
    - Exercises the Credit Cards RAG chunks, generates interactive UI with data grids and buttons
  3. Generate a mortgage affordability calculator with income inputs, amortization schedule, and monthly payment breakdown
    - Heavy JS generation (math logic), form inputs, dynamic rendering — tests the 14B model's code generation depth
  4. Design a personal loan application wizard with income verification, loan amount slider, rate preview, and submission review
    - Multi-step wizard pattern, slider interactions, form validation — tests complex component generation
  5. Build an investment portfolio overview with asset allocation pie chart, account balances, recent transactions table, and rebalance button
    - Data visualization, tables, interactive elements — pushes the generator on diverse UI patterns