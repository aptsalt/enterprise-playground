# GPU-only dependencies for fine-tuning (RunPod / vast.ai / local RTX 4090)
# Install: pip install -r requirements-gpu.txt
torch>=2.2.0
transformers>=4.47.0
peft>=0.14.0
datasets>=3.2.0
bitsandbytes>=0.45.0
trl>=0.13.0
accelerate>=1.2.0
pyyaml
rich
click
